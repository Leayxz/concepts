apiVersion: v1
kind: Service
metadata:
  name: csharp-api-service

spec:
  selector:
    app: csharp-app
  ports:
    - port: 80
      targetPort: 8080
  type: NodePort

# O SERVICE.YML ATUA COMO LOAD BALANCER, DESCOBRINDO PODS PELO LABEL
